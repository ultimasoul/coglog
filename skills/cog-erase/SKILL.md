---
name: cog-erase
description: "Permanently remove a topic from the knowledge base and add its pattern to filterPatterns to prevent re-ingestion. Irreversible — deletes files."
---

Coglog skill directory: `${CLAUDE_SKILL_DIR}/../coglog/`

Read `${CLAUDE_SKILL_DIR}/../coglog/SKILL.md` for the full CogLog instruction set, then execute the `/cog-erase` instructions (Section ## 9).
