---
name: cog-status
description: "Quick CogLog status report: raw queue count, archive count, wiki file count, config state."
---

Coglog skill directory: `${CLAUDE_SKILL_DIR}/../coglog/`

Read `${CLAUDE_SKILL_DIR}/../coglog/SKILL.md` for the full CogLog instruction set, then execute the `/cog-status` instructions (Section ## 2).
