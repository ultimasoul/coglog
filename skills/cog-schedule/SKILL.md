---
name: cog-schedule
description: "Set up, inspect, or remove OS-native automatic scheduling of ingest.js. Subcommands: /cog-schedule N (hours), status, off."
---

Coglog skill directory: `${CLAUDE_SKILL_DIR}/../coglog/`
Scripts directory (COGLOG_SCRIPTS_DIR): `${CLAUDE_SKILL_DIR}/../coglog/scripts/`

Read `${CLAUDE_SKILL_DIR}/../coglog/SKILL.md` for the full CogLog instruction set, then execute the `/cog-schedule` instructions (Section ## 10). When the instructions reference `COGLOG_SCRIPTS_DIR`, use the scripts directory path above.
