---
name: cog-digest
description: "Structure raw thinking sessions into wiki documents (ADRs, Bug post-mortems, Domain logic) with YAML frontmatter and context_refs."
---

Coglog skill directory: `${CLAUDE_SKILL_DIR}/../loader-coglog/`

Read `${CLAUDE_SKILL_DIR}/../loader-coglog/SKILL.md` for the full CogLog instruction set, then execute the `/cog-digest` instructions (Section ## 3).
