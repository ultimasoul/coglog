---
name: cog-prune
description: "Remove low-value noise from the raw queue (< 50 words, trivial content). Adds pruned paths to state.json to prevent re-ingestion."
---

Coglog skill directory: `${CLAUDE_SKILL_DIR}/../coglog/`

Read `${CLAUDE_SKILL_DIR}/../coglog/SKILL.md` for the full CogLog instruction set, then execute the `/cog-prune` instructions (Section ## 6).
