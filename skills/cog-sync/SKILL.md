---
name: cog-sync
description: "Run the full CogLog pipeline: ingest → status → digest → map → check → prune. One command for a complete knowledge base refresh."
---

Coglog skill directory: `${CLAUDE_SKILL_DIR}/../coglog/`

Read `${CLAUDE_SKILL_DIR}/../coglog/SKILL.md` for the full CogLog instruction set, then execute the `/cog-sync` instructions (Section ## 7).
